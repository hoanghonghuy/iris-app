/**
 * Teacher Chat Page
 * Màn hình nhắn tin (Direct / Group)
 */
import ChatPage from "@/components/shared/ChatPage";

export default function TeacherChatPage() {
  return (
    <div className="-m-4 md:-m-6 lg:-m-8 h-[calc(100dvh-4rem)] md:h-[calc(100vh-4rem)]">
      <ChatPage />
    </div>
  );
}
